services:
  - type: web
    name: fundamentals-terminal
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SEC_USER_AGENT
        sync: false
